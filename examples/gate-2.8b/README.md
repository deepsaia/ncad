# gate-2.8b: wrap (emboss / engrave)

- `engraved_nameplate.hocon` - engraved text on a plate's top face (a nameplate).
- `embossed_logo.hocon` - an embossed sketch profile placed off-center with a rotation.

Both are slow STEP round-trips (see `tests/build/test_wrap.py`).
