# gate-2.7: shell + draft

- `shelled_box.hocon` - a box hollowed to a 2mm wall with an open top (`openings = top`).
- `drafted_boss.hocon` - a boss with its vertical faces drafted 5 degrees about the XY base.

Both are slow STEP round-trips (see `tests/build/test_shell_draft.py`).
