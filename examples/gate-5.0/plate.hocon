schema_version = 2
units = mm
parts {
  plate { profile = solid, features = [
    { id = sk, op = sketch, plane = XY, elements = [ { id = r, type = rectangle, w = 60, h = 40 } ] }
    { id = ext, op = extrude, profile = sk, distance = 6 }
  ] }
  peg { profile = solid, features = [
    { id = sk, op = sketch, plane = XY, elements = [ { id = c, type = circle, d = 8 } ] }
    { id = ext, op = extrude, profile = sk, distance = 20 }
  ] }
}
